node_modules/
dist/
.env

# Runtime data — brand profiles, briefs, concepts, campaign results, and
# exported ad images are per-installation, not source. Delete freely; the
# server recreates empty stores on next run.
data/
exports/
.vitest-data/
coverage/
